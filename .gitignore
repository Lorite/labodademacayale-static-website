# ---- Secrets & environment (NEVER commit these) ----
.env
.env.*
!.env.example
*.key
*.pem
*.p8
secrets.json
*.secret

# ---- Bank / payment information ----
*.iban
banking/

# ---- Guest RSVP submissions (kept off the repo) ----
data/
*.jsonl

# ---- Private photos & media ----
# Keep family photos off the public repo. Decorative styling is done in CSS,
# so the committed site still looks complete without any of these.
*.jpg
*.jpeg
*.png
*.gif
*.webp
*.mp4
*.mov
pictures/
photos/
# Lightbox photo descriptions (personal text — kept off the repo like the photos)
protected/images/captions.json

# ---- Dependencies & build output ----
node_modules/
dist/
build/
.next/

# ---- Logs ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---- Editor / OS ----
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
